# ESP32 硬件中转方案全景设计与实施规划
## 小米蓝牙语音遥控器 → USB 复合设备（免驱实体麦克风 + 独立多媒体键盘）

> **文档版本**：v1.0  
> **适用场景**：通过 ESP32 硬件做中继桥接（BLE Central ➔ USB Composite Device），彻底解决 Windows 内核键盘驱动丢弃按键、物理键盘冲突、虚拟声卡依赖等所有痛点。

---

## 一、 项目背景与解决的核心痛点

在纯 Windows 软件方案中，受限于 Windows 操作系统与蓝牙驱动架构，存在以下无法逾越的物理限制：
1. **三大按键被内核丢弃**：小米遥控器的音量加（`0x80`）、音量减（`0x81`）、返回键（`0xF1`）采用 Android 键盘编码，Windows 自带的 `kbdhid.sys` 键盘驱动会在内核层将其静默丢弃，用户态完全无法拦截；
2. **物理键盘键位冲突**：Windows 低级键盘钩子（`WH_KEYBOARD_LL`）无法区分按键是来自遥控器还是物理键盘（如回车与 OK 键冲突）；
3. **依赖虚拟声卡（VB-Cable）**：纯软件方案必须在电脑上安装虚拟音频驱动，且容易受系统休眠唤醒时的音频设备漂移影响。

### 🌟 ESP32 硬件中转方案的降维打击优势
* **100% 全按键可用**：ESP32 直接以 BLE 主机身份读取遥控器的所有原始 HID 字节，音量±和返回键毫无保留地全部接收并转换为标准 PC 键值；
* **真·物理免驱麦克风**：ESP32 板载芯片实时解码 ADPCM 音频，并通过 USB Audio Class (UAC 1.0) 模拟成一个**真实的 USB 物理麦克风**，电脑即插即用，彻底告别 VB-Cable；
* **硬件级设备隔离**：电脑将 ESP32 识别为一个独立的 USB 硬件外设，与你桌面上的主键盘物理隔离，绝不产生按键冲突；
* **极速休眠唤醒**：ESP32 作为嵌入式硬件常开常驻，遥控器唤醒直连 ESP32，电脑端仅需识别稳定的 USB 设备，无惧 PC 睡眠唤醒。

---

## 二、 硬件选型与技术规格

### 1. 推荐核心芯片：**ESP32-S3**（强烈推荐 ⭐⭐⭐⭐⭐）
* **CPU 算力**：Xtensa® 双核 32 位 LX7 微处理器，主频高达 240MHz（带矢量指令扩展，音频实时解码仅占 2% CPU）；
* **无线协议**：Bluetooth 5.0 (BLE) + 高性能板载天线；
* **关键硬件特性**：**原生内置 Full-Speed USB OTG 控制器**（支持硬件级模拟 USB 复合设备：UAC 麦克风 + HID 键盘 + CDC 串口）；
* **价格与形态**：迷你 ESP32-S3 开发板（带 Type-C 口），成本仅约 15~25 元，尺寸如无线鼠标接收器（Dongle）般小巧。

> ⚠️ **避坑提示**：
> - **不要买经典 ESP32（ESP32-D0WDQ6 等）**：经典 ESP32 不具备原生 USB 控制器，只能通过 CH340/CP2102 转串口，无法模拟 USB 麦克风和 USB 键盘。
> - **开发板接口注意**：ESP32-S3 开发板通常有两个 Type-C 口（一个写着 `UART`，一个写着 `USB/OTG`），后续烧录和插电脑必须连接 **`USB/OTG`（即 GPIO 19/20 引出的原生 USB 口）**。

---

## 三、 系统整体架构设计

```text
┌─────────────────────────────────────────────────────────────┐
│                  小米蓝牙语音遥控器 (BLE Peripheral)        │
└──────────────┬───────────────────────────────┬──────────────┘
               │ (HID 原始按键报文)              │ (16kHz ADPCM 音频流)
               ▼                               ▼
┌─────────────────────────────────────────────────────────────┐
│                ESP32-S3 固件系统 (BLE Central)              │
│                                                             │
│  1. BLE HOGP Client 模块: 捕获全量 12 键物理原始输入          │
│  2. ATVV Controller 模块: 管理 0x0A/0x0C/0x04 握手与流控制   │
│  3. DSP 音频解码引擎: IMA-ADPCM ➔ 16kHz PCM + Declip/滤波   │
│  4. 键位映射引擎: 将 0x80/0x81/0xF1 等映射为标准 PC HID      │
│                                                             │
│                TinyUSB 复合外设协议栈 (USB Device)           │
│   ┌──────────────────────┬──────────────────────────────┐   │
│   │ USB Audio (UAC 1.0)  │  USB HID Composite Device    │   │
│   │ 16kHz 16-bit 麦克风  │  标准键盘 + Consumer 多媒体  │   │
│   └──────────────────────┴──────────────────────────────┘   │
└──────────────────────────────┬──────────────────────────────┘
                               │ (单根 USB 数据线连接 PC)
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                        Windows 10 / 11 电脑                 │
│                                                             │
│  • 音频输入: 识别为【Mi Remote Wireless Mic】(原生免驱麦克风)│
│  • 输入设备: 识别为【标准 USB 键盘 & 多媒体控制器】          │
│  • 语音输入法: 麦克风直接选择该硬件，一键秒级录入            │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、 核心功能模块设计

### 模块 1：BLE 遥控器通信与 ATVV 协议栈
1. **自动扫描与绑定**：
   - 扫描广播名为 `MI RC` 或 MAC 前缀为 `C0:5D:39` 的 BLE 设备；
   - 建立连接并持久化存储绑定信息（Bonding / NVS）。
2. **HID Over GATT (HOGP) 服务监听**：
   - 订阅 HID Report 特征值（UUID: `0x2A4D`）；
   - 捕获按键：音量加（`0x80`）、音量减（`0x81`）、返回键（`0xF1`）、方向键、OK、电源、主页、菜单、电视键。
3. **ATVV 语音服务交互（UUID: `ab5e0001-...`）**：
   - 订阅 `ab5e0004` (CTL 控制通道) 与 `ab5e0003` (AUDIO 音频流通道)；
   - 发送 `0x0A` (`GET_CAPS`) 与 `0x0C` (`MIC_OPEN`) 握手；
   - 监听 `0x04` (`AUDIO_START`) 触发录音，监听 `0x00` (`AUDIO_STOP`) 结束录音。

---

### 模块 2：IMA-ADPCM 流式音频解码引擎
1. **解码算法**：
   - 采用 4-bit IMA-ADPCM 解码（Hi-nibble first，状态跨帧连续）；
   - 每 120 字节音频帧解码为 240 个 16-bit PCM 采样点（16000Hz 采样率）；
2. **音频后处理**：
   - Declip 单点尖峰消除滤波；
   - 3-Tap 三角 FIR 低通平滑滤波；
   - 软削波与 AGC 自动增益控制；
3. **环形缓冲区（Ring Buffer）**：
   - 将解码后的 PCM 送入 TinyUSB 音频 FIFO 队列，平滑供给 USB 主机拉取，防止音频抖动与爆音。

---

### 模块 3：TinyUSB 复合设备（Composite USB）
1. **USB Audio Class 1.0 (UAC 麦克风)**：
   - 描述符配置：`Audio Control Interface` + `Audio Streaming Interface`（IN 端点，Isochronous 模式，16kHz, 16-bit Mono）；
   - 在 Windows 系统声音设置中显示为独立的物理硬件麦克风。
2. **USB HID 键盘与多媒体控制器**：
   - **Keyboard Interface**：发送标准按键与快捷键（如 `Win+D`, `Alt+Tab`, `Space`, 自定义组合键）；
   - **Consumer Control Interface**：发送系统多媒体指令：
     - 小米音量加 ➔ 发送 Consumer `0x00E9` (Volume Increment)
     - 小米音量减 ➔ 发送 Consumer `0x00EA` (Volume Decrement)
     - 小米返回键 ➔ 发送 Consumer `0x0224` (AC Back) 或 Keyboard `Escape`
3. **USB CDC 虚拟串口（可选）**：
   - 用于 Web 网页端通过 WebSerial 进行可视化图形化改键与固件升级。

---

## 五、 按键重映射规则设计（默认推荐）

| 遥控器物理按键 | 小米原始 HID 报文 | ESP32 转换后向 PC 输出的 USB 信号 | 功能效果说明 |
| :--- | :--- | :--- | :--- |
| **音量+ 键** | `0x80` (Usage 128) | **USB Consumer: `Volume Up`** | 原生调节 Windows 系统主音量 (+) |
| **音量- 键** | `0x81` (Usage 129) | **USB Consumer: `Volume Down`** | 原生调节 Windows 系统主音量 (-) |
| **返回键** | `0xF1` (Usage 241) | **USB Consumer: `AC Back` / `Esc`** | 网页/文件管理器后退、播放器退出全屏 |
| **语音键 (按住)** | ATVV `AUDIO_START` | **注入语音热键 (如 `RAlt+,` 或 `F8`) + USB 麦克风开始推流** | 自动呼出语音输入法并实时录音 |
| **语音键 (松开)** | ATVV `AUDIO_STOP` | **释放语音热键 + USB 麦克风停止推流** | 结束语音录入并转写文字 |
| **电源键** | `0xFF` / `0x66` | **USB Keyboard: `Alt + Tab` (长按关屏)** | 任务切换 / 息屏控制 |
| **主页键** | `0x24` / `0x4A` | **USB Keyboard: `Win + D`** | 一键显示桌面 |
| **菜单键** | `0x5D` / `0x65` | **USB Keyboard: `Space` (空格键)** | 播放器播放 / 暂停 |
| **直播/TV 键** | `0xC0` / `0x35` | **自定义快捷键 (如呼出常用软件)** | 自由绑定 |
| **方向上/下/左/右** | `0x52 / 0x51 / 0x50 / 0x4F` | **USB Keyboard: `Up / Down / Left / Right`** | 视频快进、快退、调进度 |
| **确定键 (OK)** | `0x28` | **USB Keyboard: `Return / Space`** | 确定 / 播放暂停 |

---

## 六、 固件开发技术选型与工具链

### 推荐方案：**ESP-IDF v5.x + TinyUSB + NimBLE**
* **理由**：
  - ESP-IDF 是乐鑫官方底层 C/C++ SDK，对 ESP32-S3 原生 USB OTG (TinyUSB) 的支持最完善；
  - `NimBLE` 蓝牙协议栈比原生 Bluedroid 节省 50% 以上的 RAM 内存，GATT 吞吐性能更高，延迟低于 10ms；
  - 官方内置了成熟的 `tusb_audio` 和 `tusb_hid` 复合设备示例代码。

---

## 七、 详细实施路线图（分阶段任务清单）

等你的 ESP32-S3 开发板到货后，我们在新会话中按照以下 6 个步骤逐步落地：

### 阶段一：硬件点亮与 USB 复合设备原型搭建
- [ ] 搭建 ESP-IDF / PlatformIO 开发编译环境；
- [ ] 配置 TinyUSB 描述符（USB Composite: UAC 麦克风 + HID 键盘 + Consumer 多媒体）；
- [ ] 将板子插上电脑，验证 Windows 设备管理器能否成功识别出“实体麦克风”和“HID 键盘”。

### 阶段二：BLE Central 扫描与遥控器配对绑定
- [ ] 编写 NimBLE 扫描与自动连接逻辑（匹配 `MI RC`）；
- [ ] 实现配对绑定密钥持久化存储（存入 NVS Flash，下次开机自动秒连）；
- [ ] 抓取并打印遥控器按键的原始 HID 字节包。

### 阶段三：全按键重映射与 USB HID 发送验证
- [ ] 编写键值转换表（将 `0x80`, `0x81`, `0xF1` 转换为 Consumer / Keyboard 报文）；
- [ ] 测试音量加、音量减、返回键在 Windows 下能否完美调节系统音量和浏览器后退。

### 阶段四：ATVV 协议握手与 16kHz ADPCM 解码
- [ ] 订阅 ATVV `ab5e0004` (CTL) 与 `ab5e0003` (AUDIO) 特征值；
- [ ] 移植已验证成熟的 IMA-ADPCM 解码算法与 FIR 滤波算法到 C 语言；
- [ ] 实现 Ring Buffer 音频队列管理。

### 阶段五：USB Audio 实时推流与语音热键联动
- [ ] 将解码后的 PCM 音频流平滑喂入 TinyUSB UAC 麦克风端点；
- [ ] 联动按键：按住语音键 ➔ 发送语音热键 + 麦克风推流；松开 ➔ 释放热键 + 停止推流；
- [ ] 在微信输入法/讯飞输入法中实测语音转写流畅度与音质。

### 阶段六：封装、固件优化与 Web 可视化改键（可选进阶）
- [ ] 加入 WebSerial 网页端配置工具（插上 USB 打开网页即可改键）；
- [ ] 3D 打印一个小外壳，做成标准的 USB 适配器插头。

---

## 八、 启动新对话指引

当你的 ESP32 开发板到货后：
1. 开启一个全新对话；
2. 直接输入：**“按照 ESP32_HARDWARE_BRIDGE_PLAN.md 文档，开始第一阶段的固件开发”**；
3. 我将一步一步带你完成代码编写、编译、烧录与实机联调！
